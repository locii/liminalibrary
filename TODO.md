# LiminaLibrary — Todo

## Up Next

- [ ] In Limina Library. In the duplicate match section of the track properties we need to provide the ability to unlink the currentkly assigned file to a m4b track. We also need to make it possible to unlink / remove the current file When a user clicks This is the right track - unlink the other I think we should provide the ability to unlink or remove the other/s

- [ ] In the duplicate match panel it's impossible to read the full text of some tracks because the name is so long. Lets create a tooltip that allows you to hover and see all of the details using bg-black and rounded corners & p-2.

- [ ] When a potantial match is suggested in the pending match block in the track panel. Add a note of how many files are already attached to that listing if it has any. Lets also add a preview button so you can preview the m4b track to double check its the same track

- [ ] Prevent ability for keyboard to trap buttons mainly in the toolbar. Ie click a button then press an arrow key and the buttons is shown focussed

- [ ] Use left & right arrow keys to skip to the prev or next track when track playing

- [ ] Remind me what the timing is off regetting the m4b catalogue? If the app is open for more than an hour perhaps we should refetch?

- [ ] Add create playlist in Limina Studio and open button at the top near the playlist title
